/**
 * Framework stubs — expanded in later phases.
 * Validation engine will compose ff.validators + server rules.
 * Theme/Permission/BusinessRule services already live under infrastructure/.
 */
export {};
